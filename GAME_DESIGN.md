# Mass War Sandbox 产品总策划案

> 版本：V1.0 产品总纲
> 更新时间：2026-09-06
> 工作名：Mass War Sandbox（GPU 海量单位战争沙盒）

本文定义这款游戏最终要交付的产品形态、玩法边界和开发优先级。它是产品层总纲，
不替代引擎模块 README、数据契约或具体技术方案；当实现细节有多种选择时，优先选择
能满足本文产品目标、且不扩大引擎改动面的方案。

## 1. 产品定义

Mass War Sandbox 是一款面向 PC 的单机实时战争沙盒。玩家不经营国家，也不操控单个英雄，
而是作为战场指挥官配置多支军团、安排部署、下达有限而清晰的战术命令，然后以观战镜头
观察数万至十万级单位之间的推进、接战、远程火力和战场结果。

产品的核心承诺是：

> **让玩家在几分钟内配置一场大规模战斗，在战斗中能真正影响军团行为，
> 并且看得懂为什么胜负。**

它首先是一款“可玩、可重复、可观察”的战争沙盒，也是 GPU 海量单位技术的最终用户体验，
而不是一套只展示帧率的工程样例。

## 2. 产品目标与支柱

### 2.1 产品目标

V1 要完成一个可独立演示和反复游玩的战场闭环：

1. 选择或配置战场。
2. 配置军团、兵种、兵力、出生位置和阵型。
3. 保存多个部署方案并随时载入。
4. 开战后指挥军团移动、进攻、防守或撤退。
5. 用镜头、战术地图和遥测理解战场变化。
6. 通过歼灭或据点规则结束战斗，查看结果并快速重开。

V1 不追求内容数量、复杂经济系统或长期养成，而追求上述闭环稳定、清楚、可重复。

### 2.2 五个产品支柱

| 支柱 | 玩家感受 | 实现要求 |
|---|---|---|
| 规模 | 战场确实像战争，而不是几十个单位的放大版 | 默认档 50,000 vs 50,000，约 100,000 单位 |
| 指挥 | 命令会改变军团行为，而不是只改变 UI 文案 | 每支军团有独立目标、流场、姿态和运行时状态 |
| 可读 | 玩家能看懂前线、火力、伤亡和胜负原因 | 阵营颜色、目标标记、战术地图、结果报告统一表达 |
| 可重玩 | 同一套资产可以快速试不同阵容和部署 | 编辑器方案保存/载入、军团编排和场景复用 |
| 稳定 | 大规模运行时的限制是明确的，不制造假确定性 | 以性能基线为档位，不为非阻塞技术债打断玩法主线 |

## 3. 目标用户与使用场景

### 3.1 目标用户

- 喜欢大规模战争、阵型和战场观战的策略玩家。
- 想研究群体运动、GPU 模拟和渲染表现的技术用户。
- 需要一个可快速展示海量单位能力的演示场景。

### 3.2 不服务的用户需求

- 需要完整国家经营、科技树、外交和资源链的大战略玩家。
- 需要英雄养成、装备掉落和角色剧情的 RPG 玩家。
- 需要联机对战、观战同步或严格回放确定性的竞技玩家。

这些方向未来可以另立产品线，但不纳入 V1 的交付承诺。

## 4. 核心体验循环

### 4.1 单局循环

```text
选择场景
  -> 配置军团与兵种
  -> 调整部署方案并保存
  -> 进入战场部署阶段
  -> 开战并下达命令
  -> 观察前线、伤亡和据点状态
  -> 胜负结算
  -> 重开、换方案或返回编辑器
```

一局应当允许玩家先观察默认战斗，再逐步介入。开战前不要求玩家理解 GPU、流场、LOD
或其他引擎概念；这些是实现手段，不是玩家需要学习的规则。

### 4.2 首次体验路径

首次打开产品时，玩家应能：

1. 直接进入默认场景，不必先创建资产。
2. 在部署阶段看到至少两支军团和清楚的阵营颜色。
3. 按一次 `Enter` 开战，观察战线接触和远程攻击。
4. 用 `Space` 暂停，用 `A/M/H/R` 改变选中军团的行为。
5. 在结算时看到胜者、胜因、存活数和主要战斗时间。

### 4.3 重玩路径

玩家完成一局后，可以：

- 一键重开相同战场。
- 返回编辑器修改兵力或出生中心。
- 增加第三支军团或复制一种兵种编成。
- 保存方案 A、方案 B，并比较不同部署结果。
- 切换歼灭战和据点战。

V1 通过“阵容与部署组合”提供重玩价值，不依赖数值养成和随机掉落。

## 5. 战斗规则

### 5.1 军团与兵种

产品中的“军团”对应一个 `teamId`。同一军团可以包含多个兵种编成；编成决定兵种资源、
兵力和部署意图。引擎不额外引入玩家不可见的 `groupId`。

每种兵种由以下设计维度组成：

- 身份：名称、阵营和行为类。
- 出生：兵力、出生中心、阵型密度、宽深比和手动脚印。
- 移动：速度、转向和移动响应。
- 群体：分离、密度和编队保持。
- 战斗：攻击距离、伤害、攻击间隔和目标规则。
- 表现：动画、渲染材质、LOD 和弹道表现。

V1 的核心兵种角色：

| 角色 | 战场作用 | 当前产品定位 |
|---|---|---|
| 近战线列 | 接触前线、承受和输出近距离伤害 | 默认主力，保证战线存在感 |
| 远程火力 | 在接触前削减敌军并制造火力方向 | 默认远程单位，强化战场层次 |
| 混编军团 | 将近战和远程编成放入同一军团 | 验收重点，验证编成与指挥闭环 |
| 独立军团 | 具有单独目标和战术姿态的第三方力量 | 三队混战和据点战的基础 |

新增兵种优先通过配置资产完成；只有产品确实需要新的行为模型时，才新增引擎模块。

### 5.2 玩家命令

V1 保留四种高辨识度命令：

| 命令 | 作用 | 预期反馈 |
|---|---|---|
| 进攻 `A` | 向动态敌情推进并主动接战 | 军团目标转为敌军方向，流场更新 |
| 移动 `M` | 前往玩家点击的静态位置 | 地面显示阵营颜色的目标十字 |
| 防守 `H` | 关闭主动导航但保留近身接战 | 军团停止追击，前线保持可见 |
| 撤退 `R` | 返回该军团的出生中心 | 目标回到初始位置，可显示撤退路线 |

支持 `Shift` 追加移动航点；支持暂停、倍速和重开。V1 不加入复杂技能栏、逐单位点选、
即时编队拖拽或需要玩家高频操作的 RTS 微操。

### 5.3 游戏模式

#### 歼灭战

军团根据命令寻找并消灭敌对军团。敌方全部失去有效战力后结束战斗。
两军团时显示攻方/守方胜利；三军团及以上显示具体 `teamId` 对应的获胜军团。

#### 据点战

所有已部署军团默认向中央据点推进。单个军团独占据点时推进占领进度；多个军团同时进入
时进入争夺，捕获暂停；据点空置时进度缓慢回到中立。任一军团完成占领或提前满足歼灭条件
时结束战斗。

#### 后续候选模式

只有当当前两种模式已经稳定，才考虑限时战、护送或多据点模式。新模式必须复用现有军团、
遥测和结算表达，不能为一次规则实验重写战斗管线。

## 6. 战场与内容结构

### 6.1 场景组成

一个可交付场景由以下部分组成：

- 战场尺寸和可行走区域。
- 静态障碍与视觉呈现。
- 游戏模式与胜负参数。
- 控制点位置和占领参数（据点战）。
- 可用兵种和军团编成。
- 一个或多个部署方案。
- 战术地图、镜头和 UI 的显示元数据。

当前工程已经具备军团、方案和运行时规则的主要基础；下一阶段把规则和障碍从场景脚本
中归档为可复用的场景配置，最终让玩家通过场景选择进入游戏，而不是依赖手工打开某个 Unity 场景。

### 6.2 内容节奏

V1 不用大量地图掩盖系统不完整。建议内容顺序：

1. 一个默认开放战场：验证规模、镜头和基础命令。
2. 一个障碍战场：验证绕行、路线和局部拥挤。
3. 一个据点战场：验证争夺、占领和多军团胜负。
4. 一个混编挑战场：验证兵种组合和部署方案差异。

每个场景必须至少有一个可直接运行的默认方案；玩家可以从默认方案复制出自己的版本。

## 7. 编辑器与运行时体验

### 7.1 战场编辑器

编辑器负责“准备战斗”，不负责运行中的命令。核心能力：

- 选择场景 Manager、Scenario 和系统配置。
- 选择已保存兵种模板，添加兵种编成。
- 自动分配下一个未使用的军团 `teamId`，或直接修改现有 `teamId`。
- 修改兵力、出生中心、阵型密度、宽深比和手动脚印。
- 用固定交战间距执行 Auto-Fit。
- 另存、覆盖、载入部署方案，并支持一次 Undo 撤销整次载入。
- 归档游戏模式、控制点和障碍布局后，支持场景级方案复用。

资产规则：

- 复制编成时创建独立的 `UnitTypeConfig` 和 `SpawnConfig`。
- 移除编成只解绑当前 Scenario，不删除仍可复用的资产。
- 运行时只读配置资产，编辑器写入必须在非 Play Mode 完成。
- 依赖缺失时拒绝操作，不做部分载入或半完成保存。

### 7.2 运行时 HUD

运行时 HUD 必须优先显示玩家下一步需要的信息：

- 当前选中军团和全部军团的存活数/初始兵力。
- 当前命令、目标和据点兵力。
- 开战、暂停、倍速、重开和模式状态。
- 战术地图中的军团范围、质心、目标和镜头位置。
- 结算时的胜者、胜因、战斗时长和存活数。

技术遥测如流场重建、GRID OVERFLOW 和 LOD 统计放在诊断区域，不应遮挡主要战场信息。

### 7.3 镜头

镜头提供三种常用视角：

- 跟随某一军团。
- 同时观察双方或多军团战场。
- 自由飞行查看局部战线。

玩家进行右键、中键、Alt 或滚轮操作时，镜头跟随应立即退出，把控制权还给玩家。
镜头异常输入不得造成 NaN、Infinity 或极远坐标。

## 8. 性能与平台目标

### 8.1 平台策略

V1 优先支持带 Compute Shader 图形设备的 Windows PC。移动端、主机和浏览器不是当前交付目标；
在没有真实设备与图形 API 采样前，不承诺这些平台的帧率。

### 8.2 产品档位

| 档位 | 每方兵力 | 总单位 | 产品用途 |
|---|---:|---:|---|
| 轻量 | 10,000 | 20,000 | 编辑器迭代、低配机演示 |
| 默认 | 50,000 | 100,000 | 产品主档，目标约 30 FPS |
| 压力 | 100,000 | 200,000 | 容量和性能展示，不承诺稳定帧率 |
| 极限 | 200,000 | 400,000 | 技术展示，不作为玩法质量基线 |

默认档以 `WarSandbox.unity` 当前实配和 `PerformanceBaseline.md` 为准。压力档出现局部
GRID OVERFLOW 或视觉穿插时，必须在 UI 中明确提示，不能宣传为完全精确模拟。

### 8.3 性能决策原则

- 先保证 10k 和 50k 档的玩法闭环，再考虑更大规模。
- 非阻塞的弹道分配、LOD 确定性和局部拥挤问题记入技术债，不打断产品阶段。
- 只有当一个问题阻塞默认档、导致崩溃、错误胜负或主要玩法不可见时，才升级为当前任务。

## 9. V1 范围边界

### V1 必须有

- 多军团独立导航和敌对判定。
- 近战、远程和混编军团。
- 歼灭战和据点战。
- 战场编辑器、部署方案保存/载入、军团/兵种编排。
- 运行时命令、暂停、倍速、重开、镜头跟随和战术地图。
- 结算报告和基础诊断提示。
- 一个默认场景以及至少两个可复用的场景/方案变体。

### V1 可以有

- 命中特效、音效、屏幕震动和更丰富的弹道拖尾。
- 更多兵种外观和阵营主题。
- 更好的场景缩略图、方案筛选和新手提示。
- 简单的战斗统计比较，不保存严格回放。

### V1 明确不做

- 国家经济、资源、科技树、外交和战役地图。
- 英雄单位、装备掉落、等级成长和抽卡。
- 联机、跨机器同步和竞技排名。
- 依赖严格确定性模拟的录像回放。
- 运行时修改配置资产或在 Play Mode 编辑编成。
- 为了技术指标而牺牲默认档可读性的 400k 玩法承诺。

## 10. 开发阶段与验收门槛

### M0：海量战斗基础（已完成）

完成 GPU 空间哈希、流场、群体运动、战斗、弹道、LOD/VAT 渲染和基础遥测；默认场景能跑起来。

### M1：多军团战场闭环（已完成）

完成多军团独立导航、三队混战、据点战、运行时命令、战术地图和胜负判定，并通过真机验收。

### M2：战场编排与方案复用（当前）

完成部署方案保存/载入和军团/兵种编排入口。验收标准：

- 三队混编可以添加、修改、移除和 Undo。
- 新编成拥有独立 SpawnConfig，模板的移动/战斗/渲染设置仍可共享。
- 三队部署方案可以保存、载入并精确恢复兵力、位置和阵型。
- Play Mode 期间不写入配置资产。

### M3：场景规则归档（下一阶段）

把歼灭/据点规则、控制点参数和障碍布局归档进可复用场景配置；提供场景选择和默认方案。
验收标准：不打开 Unity Inspector 改资产，玩家可以从场景入口选择至少两个战场并开始游戏；
场景之间的规则、障碍和方案不会串写。

### M4：战斗可读性与内容闭环

补足结果报告、场景缩略图、关键事件提示和有限的美术音效增强；建立默认场景、障碍场景、
据点场景和混编挑战场景四个可重复入口。

验收标准：首次用户能在五分钟内完成一局，能回答“谁赢了、为什么赢、还剩多少兵”。

### M5：V1 发布稳定化

完成真实目标设备采样、启动和错误提示、资产依赖检查、构建流程和回归测试；冻结 V1 范围。

验收标准：默认档在目标设备达到性能基线，主路径无崩溃、无配置污染、无不可恢复的方案损坏。

## 11. 决策与变更规则

1. `GAME_DESIGN.md` 定义产品方向，`ROADMAP.md` 定义阶段顺序，`NEXT_TASK.md` 记录当前交接。
2. 任何新需求先归类为 V1 必须、V1 可选或明确不做；没有归类前不直接改引擎架构。
3. 玩法层优先复用现有 `MassEngineManager` 公共 API；引擎数据契约只有在产品闭环被阻塞时才扩展。
4. 真机体验优先于小规模测试中的非阻塞瑕疵；测试失败先判断是测试前提错误还是引擎缺陷。
5. 每个阶段必须有可复现的验收动作和测试结果，不能只以“代码完成”作为完成标准。

## 12. 当前执行顺序

1. 完成 M2 的 Unity 编辑器真机验收并合入 PR #24。
2. 从更新后的 `main` 开始 M3，先设计场景配置边界，再实现规则/障碍归档和场景入口。
3. 用至少两个场景验证规则、障碍、军团编成和部署方案互不污染。
4. 再做 M4 的结果报告、内容入口和可选表现增强。
5. 最后进入 M5 的目标设备、构建和发布稳定化，不提前扩张引擎技术债。

本文之后的代码工作都应能回答：它服务哪个里程碑、改善哪个产品支柱、是否阻塞当前主线。
