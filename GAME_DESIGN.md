# Mass War Sandbox 产品总策划案

> 策划版本：0.1，首版执行基准；产品交付目标：V1.0。
> 更新：2026-09-06。工作名暂定，本文新增范围是策划决策，不代表功能已实现或用户已逐项验收。

## 1. 一句话产品

**一款 Windows 单机大规模战争沙盒：玩家搭建军团和战场，亲自指挥或自由观战，
用不同布阵反复试出不同战局。**

最终交付可直接运行的游戏，不是要求玩家安装 Unity 的工程样例。
Unity 编辑器用于开发者制作兵种和关卡；玩家的配兵、部署、方案保存和战斗都在成品内完成。

核心体验是“排兵布阵的选择 + 大军接战的场面 + 临场命令的影响”。
不做国家经营，不做英雄养成，不为展示更大的单位数字持续扩张引擎。

## 2. 玩家与体验目标

主要面向喜欢战争观战、阵容实验和轻量指挥的玩家；技术演示是附带价值，不是玩家必须理解的内容。
以下为待试玩验证的设计目标，不是市场调查结论：

- 首次启动直接进入一个已布置好的小规模战场，能马上开战；战场选择是一个随时可达的入口。
- 首次用户在五分钟内完成一局引导战斗；普通局目标约 3～10 分钟，自由沙盒不强制结束。
- 每局至少有三个可辨认的决定：近战/远程比例、初始站位、何时推进或转向据点。
- 结束后能看清胜者、胜因、各军团存活/损失和时间，并修改一个变量再试。
- 发布版完成全部主流程不依赖 Inspector、Console、Project 窗口或开发者辅助。

产品优先级：可玩闭环 > 命令与胜负正确 > 战场可读 > 默认规模表现 > 更多内容 > 极限规模。

## 3. 首发范围

| 维度 | V1 执行决定 |
|---|---|
| 平台 | Windows PC、键鼠、离线可玩；最低硬件配置待独立构建实测 |
| 控制方式 | 默认自由沙盒，可切换指挥任一军团，也可不干预而观战 |
| 交战规模 | 首发内容覆盖 2～4 支军团；更多军团仅属实验档，不宣称同等性能 |
| 敌我关系 | 同一军团的兵种互为友军，不同军团一律敌对；V1 不做联盟 |
| 军团含义 | 一个独立命令对象；同团混编共同接受命令，不支持同阵营拆成多支独立分队 |
| 核心兵种 | 近战线列、远程火力两类必须可辨识；先做好混编，不堆兵种数量 |
| 胜负模式 | 歼灭战、单据点战；模式与指挥权限是两个不同选项 |
| 地形 | 平面战场 + 有限 XZ 静态矩形障碍；V1 不要求高低差战术或可破坏地形 |
| 配置与保存 | 游戏内战前布阵、本地方案库；开发者模板只读，玩家修改自己的方案副本 |
| 内容 | 开放、障碍、据点、三方混编共四个可玩预设，可复用地图，不要求四张新地图 |

“四支军团”不等于四个玩家，也不自动给每团增加 50k 人。
新首发预设按全场总兵力分配规模；现有默认场景和已验收的 11 万单位样例本轮不改。

## 4. 核心循环

```text
直接进入默认战场 / 选择已有战场
  -> 战前配置（军团、兵种、人数、位置、阵型、规则）
  -> 另存方案 / 载入已有方案
  -> 开战
  -> 指挥、暂停、倍速、观战
  -> 胜负结算 / 主动结束本局
  -> 原样重开 / 返回布阵改一项 / 换战场
```

### 战前配置

玩家从内置兵种目录增加编成、分配到军团，编辑数量、出生中心、密度和宽深比。
地面显示部署脚印；输入数值与点击放置使用同一份战前数据。
修改可能造成不合法的出生位置时，显示原因并禁用开战，不悄悄重排玩家的布阵。
Auto-Fit 是明确触发的命令，不在载入或每次数值编辑后自动运行。

初版不做阵型旋转、绘制任意队形或战斗中的编成增删。队形是出生部署，不承诺移动中保持刚性军阵。
玩家可以把近战放在前、远程放在后；是否有效由真实交战表现验证，不额外承诺掩护加成。

### 战斗中

军团可接收进攻、移动、防守和撤退命令；同军团内的不同编成不单独微操。
未接到新命令的军团沿用已下达的命令，单位继续自主寻敌和战斗。
默认开战命令由当前模式决定，不将现有动态寻敌包装成高层战略 AI。

V1 先不做只允许控制一方的挑战权限、聪明的敌方指挥官或预算对战；这些是后续候选，
不是自由沙盒成立的前提。玩家可主动限制自己的干预，但游戏不把自由操控局宣称为公平挑战。

### 战后与重玩

结算记录所有实际参战军团，而不仅是攻方与守方。信息包含胜因、初始人数、存活、损失与时长。
提供原样重开、回到本局战前方案、选择其他战场三个动作。
重开从战前快照开始，不从死亡后的状态重新保存。
战局陷入僵持时可主动结束，记作“手动结束”，不冒充某方获胜。

不做必须刷取的解锁。全部首发兵种和地图直接可用，重玩动机来自改变布阵与命令。
同一方案可复用，但目前不保证每次战斗逐帧一致，统计比较不等于确定性回放。

## 5. 战斗设计

### 5.1 兵种与战术角色

| 角色 | 设计目标 | 选择与限制 |
|---|---|---|
| 近战线列 | 推进和接战时形成可读前线 | 要接触敌军才能输出；远程接敌前可能造成损失 |
| 远程火力 | 用射程和射击节奏形成战场层次 | 弹道可能落空；不能只靠加射程成为全能兵种 |
| 同团混编 | 让前后部署和数量比例影响战局 | 整团接受命令，不承诺单独指挥其中的远程编成 |
| 第三/第四军团 | 引入多方向威胁和据点争夺 | 独立敌对，无联盟或友军包夹机制 |

平衡先用数量、伤害、血量、射程、间隔和速度这些现有维度。
每次调整用同规模的近战对近战、远程对近战、混编对单一兵种和三方据点局观察效果。
参数表按试玩结果调整，不预先编造必胜克制或精确胜率。
两类兵种已经支持后，再考虑轻快近战/重型近战等配置变体，避免为数量引入新 kernel。

目前不承诺射线遮挡、护甲类别、范围伤害、弹丸途中误伤或完整物理破坏。
静态障碍首先影响通行，不能把地图包装成依赖弹道掩体的射击游戏。

### 5.2 指挥

| 命令 | 玩家可理解的含义 | 主要反馈 |
|---|---|---|
| 进攻 A | 主动向敌军推进 | 当前命令、推进方向 |
| 移动 M | 到指定位置，Shift 追加航点 | 地面落点、路线、取消待选目标 |
| 防守 H | 停止主动追击，保留近身接战 | 命令状态改变；不等同于无敌或暂停 |
| 撤退 R | 返回本团出生中心 | 明确撤回目标；不保证安全脱战 |

暂停允许改变命令，再继续观察；倍速 0.5/1/2/4 复用现有能力。
进入布阵与切换战场必须结束当前战斗并清理旧状态，不能在交战中直接修改人数和阵营。

### 5.3 胜负

- 歼灭战：只剩一个已参战军团有存活单位时获胜；全部参战军团都归零时平局。
- 据点战：单团独占时推进占领，多团争夺时暂停，空置时进度回落；换占领方先清除原进度。
- 据点战也可通过歼灭其余军团提前获胜。终局只结算一次，胜者用具体军团身份记录。
- 据点半径和占领时长来自战场定义；无效遥测不能当成所有单位阵亡。
- 只有一支有兵力的军团时，可做观战测试，但标准交战预设必须至少两方有效参战。

模式规则以代码和针对性测试落实；僵持或玩家手动结束不新增虚构胜因。

## 6. 首发内容与表现

### 四个预设

| 预设 | 核心体验 | 内容验收 |
|---|---|---|
| 开阔对冲 | 默认两军团，近战/远程接战 | 开战前可直接看清两军，远近攻击有区别 |
| 障碍绕行 | 改变通行路线和接战位置 | 障碍看得见、路线走得通，不把墙当弹道掩体 |
| 中央争夺 | 两方争夺一个据点 | 独占、争夺、中立与获胜状态清楚 |
| 三方混编 | 多方向接敌与选择干预对象 | 三方分别可选、可命令、可结算 |

这些是内容规格，不是现在已经制作完成的四张地图。优先复用现有模型、材质与场景。
首次引导使用轻量版本；完整默认档继续以 50k vs 50k 为基线。

### 美术与声音

采用现有资产支持的风格化战场方向，正式名称和题材包装暂不冻结。
阵营靠一致色彩、编号和标记区分，兵种还要靠轮廓/武器区分，不能只靠换颜色。
远景看军团范围和战线，近景看动画、近战与弹道；场景装饰不能遮住前线和据点。

首发需要基本的命令、开战、据点和结束音效，以及音量/静音设置；这些尚待实现。
海量单位的声音使用聚合表达，不为每个单位创建音源。屏幕震动、华丽拖尾和大量命中特效可选。
发布前整理随包资源清单与使用授权，不把所有第三方原始制作素材都打包给玩家。

## 7. 界面与存档

### 7.1 开发者制作工具（M2 已实现，待本轮验收）

`MassEngine/War Sandbox Editor` 负责模板资产、编成增删、布阵、Auto-Fit 和编辑器部署快照。
复制新编成生成独立 UnitTypeConfig/SpawnConfig，其余兵种参数可共享；移除不删除资产。
Unity Play Mode 期间这一套资产编辑工具保持禁用，M2 的验收规则不变。

### 7.2 玩家布阵与方案库（M4 待实现）

独立构建中的战前面板提供模板选择、军团增删、数量/位置/阵型设置、脚印预览、撤销/重做。
玩家不直接看到 `teamId`、文件 GUID、buffer 维度或 AssetDatabase；使用军团名称、兵种名称和兵力。
能查看模板详情，但修改的是本局副本，不修改内置兵种平衡。

玩家方案保存到本地用户目录，包含格式版本、战场 ID、模板 ID 和部署数值。
不保存 Unity 对象指针、进行中的 GPU 状态或录像；导入旧版本、缺失模板、非法数值时明确拒绝，
保留当前有效方案。写入成功后再替换旧文件，取消编辑不污染上次保存内容。
内置方案只读，玩家从它另存；重启游戏后仍能打开自己的方案。

技术方向：制作期配置、运行时实例、本地存档三者分离。
运行时可以修改独立的战前实例，但绝不写回项目配置资产；开战时使用同一份已验证实例初始化模拟。
这不是简单把当前 Editor-only 的 `TryApply` 改成允许 Play Mode，具体格式和生命周期在 M4 单独设计。

### 7.3 战斗 HUD 与镜头

优先显示选中军团、所有参战方兵力、当前命令、阶段、据点与时间控制。
保留战术地图、目标标记、航线、自由镜头、全景和军团跟随；用户手动操控立即退出跟随。
工具按钮用图标和提示，军团/模式用明确选择控件，避免把所有操作做成长文本按钮。
技术遥测默认收起，关键性能告警不阻挡指挥。主要内容在 1280×720 和 1920×1080 下可读且不重叠。

## 8. 性能与交付边界

| 档位 | 两方示例 | 全场总数 | 定位 |
|---|---|---:|---|
| 轻量 | 10k vs 10k | 20k | 首次体验、迭代与较低负载 |
| 默认 | 50k vs 50k | 100k | 产品主档，现有环境约 30 FPS |
| 压力 | 100k vs 100k | 200k | 可选实验，不承诺稳定帧率 |
| 极限 | 200k vs 200k | 400k | 技术展示，不作为玩法验收线 |

依据是 [现有性能基线](Assets/Game/PerformanceBaseline.md)，属于既有场景和实测环境，
不是所有 PC、所有远程比例、四军团或独立构建的帧率保证。
正式配置在 M5 记录 CPU/GPU/显存、图形 API、分辨率、构建类型和采样战局，测平均与高分位帧时间。
默认档以选定参考机约 30 FPS 为目标；具体最低硬件配置在实测前保持未定。

更多远程兵会影响弹道池，更多军团会增加流场成本；单位总数不是唯一压力来源。
保留溢出提示；默认档下影响命令、胜负或主要视觉的容量问题才升级为主线阻塞。
LOD 非确定性、微小穿插、暂停后一两帧新增曳光沿用路线图中的降级决定，不借策划案启动大修。

## 9. 范围清单

**V1 必须**：独立构建、两类兵种、2～4 团首发内容、两种胜负模式、玩家战前布阵、
本地方案库、命令与观战、全军团结算、四个可玩预设、基础音效/设置和主路径回归。

**可选增强**：更多配置型兵种、更多地图、场景缩略图、方案搜索、结果比较、精细特效。
没有它们不阻止 V1 完成，不以它们替代玩家布阵与保存。

**V1 不做**：经济/建设/科技树、战役大地图、英雄养成、联盟、同方独立分队、联机排行、
确定性回放、地形破坏、三维导航、开放模组平台，以及运行时编辑项目资产或交战中增删编成。
运行时战前编辑自己的方案明确属于 V1，不在禁止项里。

发布渠道、价格、最终名称、最低硬件规格暂未定；不为这些选择提前接入账号、支付或网络服务。

## 10. 实施里程碑

路线图中的历史编号保留，下面是产品里程碑映射，不创建另一套平行进度：

| 产品里程碑 | 对应路线图 | 状态与出口 |
|---|---|---|
| M0 海量战斗基础 | 历史引擎阶段 | 已有模拟、战斗、渲染与遥测 |
| M1 多军团与基础可读性 | 阶段 1～2 | 已实现并已有用户真机验收 |
| M2 开发者编排与快照 | 阶段 3 | PR #24 代码已完成，当前编辑器验收未完成 |
| M3 规则归档与场景入口 | 阶段 4 | 下一阶段，拆小交付 |
| M4 玩家布阵与方案闭环 | 阶段 5 | 待实现，不能以 Unity 工具替代 |
| M5 内容收口与 V1 交付 | 阶段 6 | 待实现，实际构建与试玩放行 |

### M2 的出口

新增/移除/Undo、独立 SpawnConfig、三团混编保存/载入在 Unity 内验收通过。
这一出口只说明制作工具可用，不宣称玩家版编辑器或 V1 已完成。

### M3 的三步

1. M3.1：规则、据点和障碍定义资产 + 现有编辑器入口 + Controller 只读应用。
   切换 A/B 定义与重开不串写，旧场景未绑定时保持原行为。
2. M3.2：场景目录与游戏内选择入口，使用场景中预制好的默认部署，暂不载入玩家存档。
   场景选择、失败返回、重新进入和退出需在独立构建运行，不只在 Editor 内成立。
3. M3.3：至少两个不同规则/障碍组合的战场入口，开战、结算、重开、返回均可完成。

技术预研见 [场景规则归档方案](Assets/方案设计/场景规则归档方案.md)。M3 不直接引用 Editor-only 部署方案。

### M4 的三步

1. M4.1：运行时独立部署实例与玩家布阵，开战前应用，旧 GPU 资源可正确释放与重建。
2. M4.2：模板稳定 ID、本地方案格式、另存/载入、版本与依赖错误处理。
3. M4.3：独立构建完成配兵、保存 A/B、各打一局、退出应用、重启再载入的完整循环。

### M5 的放行条件

- 四个首发预设各能走完布阵、开战、结算与重开；默认方案不可被玩家修改覆盖。
- 结算覆盖第三/第四军团；命令和战场变化有足够的视听反馈，设置能正常保存。
- 非开发者首次试玩完成轻量引导局，并能说明胜者与胜因；记录实际用时而不是自行宣称通过。
- Windows 构建不依赖 UnityEditor；缺失资源/损坏存档有可操作的错误处理，不丢失原有效文件。
- 参考设备完成默认档采样和连续切换/重开检查，无持续资源增长或主路径崩溃。
- EditMode、GPU PlayMode、场景烟测和构建烟测通过；仅文档改动不要求重跑 GPU 回归。

完成以上即为 V1 的终点；其余需求另开后续版本，不无限延长发布稳定化。

## 11. 执行规则

`GAME_DESIGN.md` 决定产品是什么；`ROADMAP.md` 决定顺序和当前阶段；
`NEXT_TASK.md` 只写交接且不进 Git；模块 README 说明当前代码而不是未来承诺。

当前入口：先完成 PR #24 的 M2 验收，再从合并后的 main 开始 M3.1。
本次只建立策划和预研文档，不自动合并 PR，也不把 M3 实现混进 M2 验收分支。

每项开发标注服务的里程碑、验收动作和明确非目标。非阻塞技术债继续延后；
若需要改变军团身份、运行时资产契约或 V1 范围，先更新策划决策再实现。
名称、内容包装与平衡参数可迭代，但不能悄悄删掉玩家版布阵、保存或独立运行的交付门槛。
